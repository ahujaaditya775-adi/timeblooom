Add `hero-poster.jpg` here (a single atmospheric still — starry sky, moonlit
clouds, or similar) — used as the poster frame for the hero background video
before it loads, and as a static fallback if video is disabled.
