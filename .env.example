# --- Supabase ---
# Found in Supabase Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://qvupbijwqotofyuvmrog.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Ufd9hQ4j3XtVtZpnS-fUFA_hVuoLTZW
# Server-only. NEVER prefix with NEXT_PUBLIC_. Used only in server routes/actions.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Storage bucket names (created automatically by supabase/schema.sql)
NEXT_PUBLIC_SUPABASE_MEDIA_BUCKET=capsule-media
NEXT_PUBLIC_SUPABASE_AVATAR_BUCKET=avatars

# --- App ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Email (optional, graceful fallback if unset) ---
# https://resend.com
RESEND_API_KEY=
RESEND_FROM_EMAIL="TimeBloom <memories@yourdomain.com>"

# --- Cron / scheduled notifications ---
# Random secret used to authorize calls to /api/cron/notify from a cron provider (e.g. Vercel Cron, cron-job.org)
CRON_SECRET=change-me-to-a-long-random-string
